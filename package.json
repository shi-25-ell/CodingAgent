{
  "name": "fast-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "build": "tsc -b --pretty false",
    "typecheck": "npm run typecheck:packages && npm run typecheck:tests",
    "typecheck:packages": "tsc -b --pretty false",
    "typecheck:tests": "tsc -p tsconfig.test.json --noEmit --pretty false",
    "lint": "biome check .",
    "print:deterministic": "node scripts/run-deterministic-print.mjs",
    "test": "vitest run",
    "test:unit": "vitest run packages/model/test/unit packages/agent/test/unit packages/coding/test/unit",
    "test:contract": "vitest run packages/model/test/contract packages/agent/test/contract packages/coding/test/contract test/contract",
    "test:integration": "npm run build && vitest run test/integration",
    "test:system": "npm run build && vitest run test/system packages/sqlite/test/system",
    "test:acceptance": "npm run build && vitest run test/acceptance",
    "test:model": "vitest run packages/model/test",
    "test:wire": "vitest run packages/model/test/contract/openai-compatible.contract.test.ts packages/model/test/unit/turn-accumulator.test.ts",
    "test:tools": "vitest run packages/coding/test/contract/coding-tool-host.contract.test.ts packages/agent/test/contract/agent-harness.contract.test.ts",
    "test:cli": "npm run build && node scripts/run-m1-deterministic-print.mjs --print \"read test/fixtures/openai/final-answer.sse\"",
    "test:coverage": "vitest run --coverage --maxWorkers=1 && node scripts/check-m2-coverage.mjs m2 M3 80 70",
    "test:boundary": "npm run build && node scripts/check-package-boundaries.mjs && node scripts/check-package-exports.mjs",
    "scan:production": "node scripts/check-production-readiness.mjs",
    "audit:dependencies": "npm audit --audit-level=moderate",
    "check": "npm run typecheck && npm run lint && npm run test:unit && npm run test:contract && npm run test:integration && npm run test:system && npm run test:acceptance && npm run test:boundary && npm run test:coverage && npm run scan:production && npm run audit:dependencies"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.2",
    "@types/node": "24.3.0",
    "@vitest/coverage-v8": "^3.2.7",
    "typescript": "5.9.2",
    "vitest": "3.2.7"
  },
  "engines": {
    "node": ">=22.18"
  }
}
